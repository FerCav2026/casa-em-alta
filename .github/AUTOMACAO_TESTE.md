# Teste da automação de agendamento

Este arquivo não afeta o site (não faz parte do build do Astro). Ele existe
só para confirmar que o workflow `publish-scheduled.yml` consegue fazer
merge + push em `main` com sucesso. Pode ser apagado a qualquer momento.
